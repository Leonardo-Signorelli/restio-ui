# Colors
button ( bg-color-base )
input ( autocomplete etc.. )
checkbox

button ( base, primary, danger)

bg-color-base --> "grey10" 
bg-color-0 --> "gray" ( to black )
overlay ( tipo list etc.. ) --> "gray0"

bg-color-0/bg-color-base hover è lo stesso
bg-color-active 
bg-color-focus

Primario a parte
Il testo è colore uguale

danger
base è sempre uguale con testo rosso
sfondo è --> Rosso trasparente sul trasparente
sfondo è --> Rosso rosso sul grigio base 


##  Border 
1 border for all, light (1 token over than bg-color-focus )
uguale per bordi button, input, divider etc..

## Text
Primary 
Secondary

## Tag
Primary, alternative color, danger, base


/**
 * Design System — Typography Tokens
 *
 * ┌──────────────┬───────────────┬────────────────┬
 * │ Property     │ Example Value │ Required?      │
 * ├──────────────┼───────────────┼────────────────┼
 * │ font-style   │ normal        │ optional       │
 * │ font-variant │ normal        │ optional       │
 * │ font-weight  │ 700           │ optional       │
 * │ font-size    │ 16px          │ mandatory      │
 * │ line-height  │ 1.2           │ optional       │
 * │ font-family  │ sans-serif    │ mandatory      │
 * └──────────────┴───────────────┴────────────────┴
 */